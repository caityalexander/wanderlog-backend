meta {
  name: blogs id comments -invalid- -parent already has parent-
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/blogs/3/comments
  body: json
  auth: none
}

headers {
  X-Authorization: token
}

body:json {
  {
      "comment": "A comment from Bruno",
      "parentId": 6
  }
}

tests {
  test("Status code is 403", function () {
      expect(res.getStatus()).to.equal(403);
  });
}
