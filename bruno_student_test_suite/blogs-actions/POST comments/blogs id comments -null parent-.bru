meta {
  name: blogs id comments -null parent-
  type: http
  seq: 8
}

post {
  url: {{BASE_URL}}/blogs/3/comments
  body: json
  auth: none
}

headers {
  X-Authorization: token
}

body:json {
  {
      "comment": "A comment from Bruno 3",
      "parentId": null
  }
}

tests {
  test("Status code is 201", function () {
      expect(res.getStatus()).to.equal(201);
  });
}
