meta {
  name: blogs id react -invalid- -DNE id-
  type: http
  seq: 3
}

get {
  url: {{BASE_URL}}/blogs/99999999/react
  body: none
  auth: none
}

tests {
  test("Status code is 404", function () {
      expect(res.getStatus()).to.equal(404);
  });
}
