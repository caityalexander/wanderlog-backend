meta {
  name: blogs id react -invalid- -invalid auth-
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/blogs/3/react
  body: json
  auth: none
}

headers {
  X-Authorization: NOT_A_VALID_TOKEN
}

body:json {
  {
    "reaction": "REACTION_1"
  }
}

tests {
  test("Status code is 401", function () {
      expect(res.getStatus()).to.equal(401);
  });
}
