meta {
  name: blogs id react -invalid- -NaN id-
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/blogs/NOT_AN_ID/react
  body: json
  auth: none
}

headers {
  X-Authorization: token
}

body:json {
  {
    "reaction": "REACTION_1"
  }
}

tests {
  test("Status code is 400 or 404", function () {
      expect(res.getStatus()).to.be.oneOf([400, 404]);
  });
}
