meta {
  name: blogs id react -invalid- -invalid reaction dne-
  type: http
  seq: 5
}

delete {
  url: {{BASE_URL}}/blogs/33/react
  body: none
  auth: none
}

headers {
  X-Authorization: token
}

tests {
  test("Status code is 403 or 404", function () {
      expect(res.getStatus()).to.be.oneOf([403, 404]);
  });
}
