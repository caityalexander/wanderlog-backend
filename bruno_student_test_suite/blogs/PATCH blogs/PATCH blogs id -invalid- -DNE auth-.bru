meta {
  name: PATCH blogs id -invalid- -DNE auth-
  type: http
  seq: 8
}

patch {
  url: {{BASE_URL}}/blogs/16
  body: none
  auth: none
}

headers {
  X-Authorization: NOT_A_TOKEN
}

tests {
  test("Status code is 401", function () {
      expect(res.getStatus()).to.equal(401);
  });
  
}
