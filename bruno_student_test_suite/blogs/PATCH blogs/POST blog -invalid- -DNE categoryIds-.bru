meta {
  name: POST blog -invalid- -DNE categoryIds-
  type: http
  seq: 7
}

patch {
  url: {{BASE_URL}}/blogs/1
  body: json
  auth: none
}

headers {
  X-Authorization: token
}

body:json {
  {
    "categoryIds": [
      999
    ]
  }
}

tests {
  test("Status code is 400", function () {
      expect(res.getStatus()).to.equal(400);
  });
}
