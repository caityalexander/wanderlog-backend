meta {
  name: POST blog -invalid- -empty title-
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/blogs
  body: json
  auth: none
}

headers {
  X-Authorization: token
}

body:json {
  {
    "title": "",
    "description": "Now its empty...",
    "cityId": 1,
    "series": "A series 123",
    "categoryIds": [
      1,
      2
    ]
  }
}

tests {
  test("Status code is 400", function () {
      expect(res.getStatus()).to.equal(400);
  });
}
