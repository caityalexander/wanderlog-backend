meta {
  name: GET blogs -invalid- -categoryIds DNE-
  type: http
  seq: 12
}

get {
  url: {{BASE_URL}}/blogs?categoryIds=9999999
  body: none
  auth: none
}

params:query {
  categoryIds: 9999999
}

tests {
  test("Status code is 400", function () {
      expect(res.getStatus()).to.equal(400);
  });
}
