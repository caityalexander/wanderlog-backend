meta {
  name: DELETE blogs id -invalid- -DNE auth-
  type: http
  seq: 4
}

delete {
  url: {{BASE_URL}}/blogs/16
  body: none
  auth: none
}

headers {
  X-Authorization: NOT_A_TOKEN
}

tests {
  test("Status code is 401", function () {
      expect(res.getStatus()).to.equal(401);
  });
  
}
